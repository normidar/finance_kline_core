## 0.0.4

- Add `cleanCombo` and `combo` to `PipeWrapper`.
- Add `nextBody`, `prevBody`, and `getBody` to `PipeWrapper`.

## 0.0.3

- Update metadata.

## 0.0.2

- Remove fpdart dependency.

## 0.0.1

- Initial release.
